% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barn.R
\name{plant_decimals_round}
\alias{plant_decimals_round}
\title{Round numeric features to specified precisions}
\usage{
plant_decimals_round(barn_obj, numeric_sufix = "_num", precisions = c(9, 8))
}
\arguments{
\item{barn_obj}{A \code{barn} object, created by \code{\link[=barn]{barn()}}.}

\item{numeric_sufix}{The suffix used to identify numeric columns to process.
Defaults to "_num".}

\item{precisions}{A numeric vector specifying the number of decimal
places to round to (e.g., \code{c(9, 8)}).}
}
\value{
The modified \code{barn_obj} with the transformed combined data frame.
}
\description{
Creates new numeric columns by rounding existing numeric columns
at specified decimal precisions. This is useful for feature engineering,
where different rounding granularities may capture meaningful patterns.
}
\examples{
df <- tibble::tibble(x_num = c(1.23456789))
b <- barn(df) |> plant_decimals_round(precisions = c(2, 3))
harvest(b)[[1]]
harvest(b)[[1]]$x_r2_num
harvest(b)[[1]]$x_r3_num
}
