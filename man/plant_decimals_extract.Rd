% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barn.R
\name{plant_decimals_extract}
\alias{plant_decimals_extract}
\title{Extract decimals in numeric features}
\usage{
plant_decimals_extract(barn_obj, numeric_sufix = "_num", from = 1, to = 10)
}
\arguments{
\item{barn_obj}{A \code{barn} object, created by \code{\link[=barn]{barn()}}.}

\item{numeric_sufix}{The suffix used to identify numeric columns to process. Defaults to "_num".}

\item{from}{The starting digit position to extract (e.g., 1 for the first decimal place). Defaults to 1.}

\item{to}{The ending digit position to extract (e.g., 9 for the ninth decimal place). Defaults to 9.}
}
\value{
The modified \code{barn_obj} with the transformed combined data frame.
}
\description{
Creates new integer columns by extracting specific digits from numeric columns.
This function emulates a feature engineering technique often used in machine learning.
}
\examples{
df <- tibble::tibble(x_num = c(1.234, 5.678, NA))
b <- barn(df) |> plant_decimals_extract(from = 1, to = 3)
harvest(b)[[1]]
}
